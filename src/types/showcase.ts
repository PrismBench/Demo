/**
 * Metadata describing a tree for the showcase.
 */
export interface TreeMeta {
  /** Unique identifier for the tree. */
  id: string;
  /** Display name of the tree. */
  name: string;
  /** Filename to fetch the tree data from. */
  file: string;
}
